export interface ResultInputDTO {
    competition: string,
    athlete: string,
    value: number;
    unit: string
};

export interface ResultInputMockDTO {
    id: string,
    competition: string,
    athlete: string,
    value: number;
    unit: string
};