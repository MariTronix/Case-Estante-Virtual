export interface ResultInputDTO {
    competition: string,
    athlete: string,
    value: number;
    unit: string
};

export interface ResultInputMockDTO {
    competition: string,
    athlete: string,
    value: number;
    unit: string
};